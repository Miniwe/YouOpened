/* 
 * @fileOverview Общение с твиттером
 */

var TRequest = Backbone.Model.extend({
    defaults: {
        query : "noQuery",
        data : "noData"
    },
    initialize : function () {
    }
});

console.log('twitter here');
